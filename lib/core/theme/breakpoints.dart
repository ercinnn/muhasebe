class Breakpoints {
  const Breakpoints._();

  /// Below this width: BottomNavigationBar (mobile layout).
  /// At or above: NavigationRail + multi-column layout (web/desktop layout).
  static const double rail = 840;
}
