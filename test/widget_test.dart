import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'placeholder — widget tests require a fake-able Supabase bootstrap',
    () {
      // App-level widget tests will be added once bootstrap() can be
      // injected with a mocked Supabase client in a later phase.
    },
    skip: true,
  );
}
